"use client";

import { useMemo, useState } from "react";
import { AdminButton, AdminListChevron, AdminListHeader, AdminListMobileCard, AdminListRow, AdminMetric, AdminModuleCards, AdminModuleEmptyState, AdminModuleFilters, AdminModuleHeader, AdminModuleList, AdminModulePage, AdminModuleSummary, AdminStatusChip } from "@/components/admin/design-system";
import { filterTeamTypes, sortTeamTypes } from "./teamTypes.core";

const columns = ["", "Bezeichnung", "Anzeigename", "Altersklasse", "Abteilung", "Status", "Sortierung", ""].map((label, index) => ({ key: `${index}-${label}`, label }));
const template = "3rem minmax(0,.7fr) minmax(0,1.2fr) minmax(0,1fr) minmax(0,.8fr) minmax(0,.7fr) minmax(0,.55fr) 2rem";
const departmentLabel = (item) => item.departments?.name_de || item.departments?.slug || "Keine Abteilung";

export default function TeamTypesModule({ teamTypes = [] }) {
  const [status, setStatus] = useState("all");
  const sorted = useMemo(() => sortTeamTypes(teamTypes), [teamTypes]);
  const visible = useMemo(() => filterTeamTypes(sorted, status), [sorted, status]);
  const summary = { total: teamTypes.length, active: teamTypes.filter((item) => item.is_active !== false).length, inactive: teamTypes.filter((item) => item.is_active === false).length };
  const cards = <AdminModuleCards className="xl:hidden">{visible.map((item) => <AdminListMobileCard key={item.id} href={`/admin/settings/team-types/edit/${item.id}`} label={`${item.name_de} bearbeiten`}><div className="flex items-start justify-between gap-3"><div className="min-w-0"><p className="font-black text-white">{item.slug?.toUpperCase()}</p><p className="mt-1 text-sm text-white/60">{item.name_de}</p></div><AdminListChevron label="Vorlage öffnen" /></div><div className="mt-3 flex flex-wrap gap-2"><AdminStatusChip compact variant={item.is_active === false ? "warning" : "success"}>{item.is_active === false ? "Inaktiv" : "Aktiv"}</AdminStatusChip><span className="text-sm text-white/55">{item.age_group}</span><span className="text-sm text-white/55">{departmentLabel(item)}</span></div></AdminListMobileCard>)}</AdminModuleCards>;
  return <AdminModulePage><AdminModuleHeader eyebrow="Einstellungen" title="Mannschaftsvorlagen" description="Verwalte die verfügbaren Mannschaftsbezeichnungen je Abteilung." actions={<AdminButton href="/admin/settings/team-types/new" variant="primary">Neue Mannschaftsvorlage</AdminButton>} /><AdminModuleSummary><AdminMetric label="Gesamt" value={summary.total} /><AdminMetric label="Aktiv" value={summary.active} /><AdminMetric label="Inaktiv" value={summary.inactive} /></AdminModuleSummary><AdminModuleFilters title="Vorlagen filtern" panelId="team-type-filters"><label className="grid max-w-xs gap-2 text-sm font-bold text-white/65">Status<select value={status} onChange={(event) => setStatus(event.target.value)} className="h-11 rounded-xl border border-white/10 bg-[#17171d] px-3 text-white"><option value="all">Alle</option><option value="active">Aktiv</option><option value="inactive">Inaktiv</option></select></label></AdminModuleFilters>{visible.length ? <AdminModuleList mobile={cards} desktopClassName="hidden overflow-hidden xl:block"><AdminListHeader columns={columns} template={template} />{visible.map((item) => <AdminListRow key={item.id} href={`/admin/settings/team-types/edit/${item.id}`} label={`${item.name_de} bearbeiten`} template={template}><span className="font-black text-white">{item.slug?.slice(0, 2).toUpperCase()}</span><span className="font-black text-white">{item.slug?.toUpperCase()}</span><span className="text-white/70">{item.name_de}</span><span className="text-white/60">{item.age_group}</span><span className="truncate text-white/60">{departmentLabel(item)}</span><AdminStatusChip compact variant={item.is_active === false ? "warning" : "success"}>{item.is_active === false ? "Inaktiv" : "Aktiv"}</AdminStatusChip><span className="text-white/60">{item.sort_order || 0}</span><AdminListChevron label="Vorlage öffnen" /></AdminListRow>)}</AdminModuleList> : <AdminModuleEmptyState title="Keine Mannschaftsvorlagen" description="Für den gewählten Filter sind keine Vorlagen vorhanden." />}</AdminModulePage>;
}
