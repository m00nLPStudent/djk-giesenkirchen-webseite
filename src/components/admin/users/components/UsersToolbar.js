import Can from "@/components/admin/auth/Can";
import { AdminModuleFilters, AdminModuleHeader, AdminModulePrimaryAction, AdminModuleSearch } from "@/components/admin/design-system";

export default function UsersToolbar({ filters, statusOptions, roleOptions, sortOptions, onSearchChange, onStatusChange, onRoleChange, onSortChange, onOpenNewUser }) {
  return <><AdminModuleHeader eyebrow="Benutzer" title="Benutzerverwaltung" description="Benutzerkonten, Rollen und Zugriffsrechte verwalten." actions={<Can permission="users.create" uiOnly><AdminModulePrimaryAction onClick={onOpenNewUser}>+ Neuer Benutzer</AdminModulePrimaryAction></Can>}><AdminModuleSearch value={filters.search} onChange={(event) => onSearchChange(event.target.value)} placeholder="Suche nach Name, E-Mail oder Rolle" label="Benutzer durchsuchen" /></AdminModuleHeader><AdminModuleFilters title="Benutzer filtern" panelId="users-filter-panel"><div className="grid gap-4 md:grid-cols-3">{[[filters.status, onStatusChange, statusOptions, "Status"], [filters.role, onRoleChange, roleOptions, "Rolle"], [filters.sort, onSortChange, sortOptions, "Sortierung"]].map(([value, onChange, options, label]) => <label key={label} className="grid gap-2 text-sm font-bold text-white/65">{label}<select value={value} onChange={(event) => onChange(event.target.value)} className="h-11 rounded-xl border border-white/10 bg-[#17171d] px-3 text-white">{options.map((option) => <option key={option.value} value={option.value}>{option.label}</option>)}</select></label>)}</div></AdminModuleFilters></>;
}
