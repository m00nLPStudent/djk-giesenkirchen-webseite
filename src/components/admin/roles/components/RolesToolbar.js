import Can from "@/components/admin/auth/Can";
import { AdminModuleFilters, AdminModuleHeader, AdminModulePrimaryAction, AdminModuleSearch } from "@/components/admin/design-system";

export default function RolesToolbar({ filters, statusOptions, sortOptions, onSearchChange, onStatusChange, onSortChange, onCreate }) {
  return <><AdminModuleHeader eyebrow="Rollen" title="Rollenverwaltung" description="Rollen und zugehörige Berechtigungen verwalten." actions={<Can permission="roles.edit" uiOnly><AdminModulePrimaryAction onClick={onCreate}>+ Neue Rolle</AdminModulePrimaryAction></Can>}><AdminModuleSearch value={filters.search} onChange={(event) => onSearchChange(event.target.value)} placeholder="Suche nach Name, Key oder Beschreibung" label="Rollen durchsuchen" /></AdminModuleHeader><AdminModuleFilters title="Rollen filtern" panelId="roles-filter-panel"><div className="grid gap-4 md:grid-cols-2">{[[filters.status, onStatusChange, statusOptions, "Status"], [filters.sort, onSortChange, sortOptions, "Sortierung"]].map(([value, onChange, options, label]) => <label key={label} className="grid gap-2 text-sm font-bold text-white/65">{label}<select value={value} onChange={(event) => onChange(event.target.value)} className="h-11 rounded-xl border border-white/10 bg-[#17171d] px-3 text-white">{options.map((option) => <option key={option.value} value={option.value}>{option.label}</option>)}</select></label>)}</div></AdminModuleFilters></>;
}
