# Aktuelle Roadmap

> B15.21A bis B15.21D11 sind abgeschlossen. Mitgliedsanfragen, saisonale Mannschaftsauflösung, Zuständigkeiten, Weiterleitung, transaktionale Membership-Mail, zentrale Notification-Mail-Delivery, globale Superadmin-Steuerung, persönliche In-App-Preferences sowie die Notification-Center-Mehrfachauswahl sind umgesetzt und verifiziert.

Stand: 28. August 2026. Dieses Dokument ist die verbindliche offene Planung. Historische B12–B15-Dateien bleiben als Nachweise erhalten, sind aber keine zweite aktuelle To-do-Liste.

Die Adminnavigation ist in die aktiven Bereiche Gesamtverein, Fußball und System gegliedert. `/admin/department` ist als „Fußballvorstände“ dem Fußballbereich zugeordnet; Mitgliedsanfragen, Medien und der gemeinsame Einstieg „Seiten, Kontakte & Einstellungen“ bleiben unter Gesamtverein. Offener separater Folgepunkt: Eine eigenständige Gesamtvereins-„Vereinsstruktur“ erst nach Analyse und Einführung einer fachlich getrennten Gesamtvereinsroute sowie einer eindeutig zugeordneten Datenquelle planen; bis dahin keinen künstlichen Navigationseintrag oder neue Route anlegen.

## Priorität 1 – Mitgliedschaft, Notifications und transaktionale E-Mails

- B15.21A–C abgeschlossen: Das öffentliche Mitglied-werden-Formular validiert Geburtsdatum und Jahrgang serverseitig, löst saisonal passende Mannschaften auf und unterstützt die gehärteten Zuständigkeits-, Sichtbarkeits-, Bearbeitungs- und Weiterleitungsworkflows.
- B15.21D3: erster kontrollierter realer Formularversand, interner Notification-Pfad, `mail_sent_at`, Resend-Verarbeitung und Postfacheingang erfolgreich verifiziert.
- B15.21D4/D4.1 abgeschlossen: Analyse und Live-Preflight bestätigten das zentrale Delivery-Modell ohne Namenskonflikt. Das server-only Ledger-Proposal wurde anschließend manuell ausgeführt und der Postcheck bestätigte RLS, fehlende Browserrechte, ausschließlichen Service-Role-Zugriff sowie unveränderte Bestandsnotifications. Details stehen unter [`b15-21d4-notification-email-delivery-architecture.md`](b15-21d4-notification-email-delivery-architecture.md).
- B15.21D5/D6 abgeschlossen: Der zentrale Notification-Service leitet ausschließlich neu persistierte, explizit freigegebene Membership-/Trainer-Notifications an den bestehenden Mail-Service aus. Der erste reale Test erzeugte genau eine `membership_forwarded`-Notification und eine erfolgreiche `sent`-Delivery mit einem Versuch, ohne Lock, Fehler, Retry oder Doppelzustellung. Resend-Verarbeitung und Postfacheingang wurden bestätigt. Kein Cron und kein automatischer Retry wurden aktiviert.
- B15.21D8/D9 abgeschlossen: Live-Preflight, manuell ausgeführtes Schema und Postcheck bestätigten `notification_email_settings`, `notification_email_global_settings` und die server-only globale Mailpolicy. Die Superadmin-Seite `System → E-Mail-Benachrichtigungen`, der standardmäßig deaktivierte Master, 27 Type-Toggles, die 16/11-Empfehlungsmatrix, 16 sichere Renderer, Bulk-Deaktivierung und Restore sind implementiert. Master/Type/Renderer bleiben dreifach default-deny; deaktivierte Entscheidungen werden terminal `skipped` und niemals rückwirkend versendet.
- B15.21D10 abgeschlossen: Die persönlichen In-App-Benachrichtigungseinstellungen verwenden eine kompakte Desktop-Tabelle mit konsistenter Spaltengeometrie und eine kompakte Mobile-Liste. Erforderliche Notifications bleiben nicht abschaltbar; optionale Toggles, „Alle optionalen aktivieren/deaktivieren“ und „Standard wiederherstellen“ verwenden unverändert die bestehende Preference-Fachlogik.
- Persönliche E-Mail-Schalter pro Benutzer sind derzeit bewusst nicht vorgesehen. Der Superadmin bestimmt zentral, welche Notification-Typen E-Mails erzeugen; persönliche `in_app_enabled`-Preferences bleiben davon getrennt.
- Go-live-Prüfung für Notification-Mail-Links: `NEXT_PUBLIC_SITE_URL` in der Deploymentumgebung auf die finale Vereinsdomain setzen und den normalisierten `/admin`-Link prüfen. Die lokale D6-Basis-/Tunnel-URL zeigte auf Port 3000, während der Testworkflow auf Port 3001 lief; dies war kein Versandfehler und wurde nicht spontan umkonfiguriert.
- Vor dem Produktivversand weiterhin Datenschutzvertrag, eigene Versanddomain, serverseitige Hosting-Secrets und Produktivumgebung verbindlich einrichten.
- B15.24N – **COMPLETE.** Das vollständige Mailinventar trennt Supabase-Auth-Templates, anwendungseigene Transaktionsmails und den vorbereiteten Contribution-Reminder. Membership-, Notification- und bestätigte Admin-E-Mail-Wechsel-Mails verwenden einen gemeinsamen providerneutralen, responsiven und escape-sicheren HTML-Vertrag mit echtem Vereinslogo sowie domainneutral erzeugten Impressum-/Datenschutzlinks. Membership und Notification sind real bestanden. Das deutsche Supabase-Recovery-Template ist im Dashboard hinterlegt und live im externen Mailclient vollständig bestanden. Das deutsche Invite-Template ist konfiguriert, wurde mangels freier geeigneter Test-E-Mail-Adresse im Abschlussreview bewusst nicht erneut live versendet und wird beim ersten echten neuen Benutzer operativ geprüft. Alle vier E-Mail-Wechsel-Renderer sind automatisiert regressionsgeprüft. Details: [`b15-24n-email-system-template-contract.md`](b15-24n-email-system-template-contract.md).
- Späterer Legal-/Footer-Ausbau: Das zentrale Grundlayout nach Freigabe der vollständigen Vereins-/Impressums-/Footerangaben bei Bedarf ergänzen. Anschrift, offizielle Kontakte, Website und gegebenenfalls Social-Media- oder rechtlich sinnvolle Hinweise dürfen nicht erfunden werden.
- Einen Idempotenz-Retry nur separat und ausdrücklich freigegeben testen; D3 hat keinen Retry ausgelöst.
- Auth-Mails und normale Vereinsmails getrennt halten. Supabase Auth verwendet in der Entwicklungs-/Übergangsumgebung erfolgreich Custom SMTP über Resend mit der verifizierten Versanddomain `mail.mavermg.de`; die spätere Umstellung auf den finalen Vereins-Mailserver und die abschließende Prüfung der produktiven Absender-/Domainkonfiguration bleiben ein eigener Go-live-Schritt.
- Keine Edge Function, SQL- oder neue Outbox-Struktur ohne einen neu nachgewiesenen Bedarf einführen.

## Priorität 2 – Download-Modul

- B15.22A/A1 abgeschlossen; B15.22B live installiert und per Read-only-Postcheck bestätigt; B15.22C abgeschlossen und manuell freigegeben. Das geschützte Admin-CRUD unter `/admin/downloads`, Navigation, Rollen, Create/Edit/Delete, Publish/Unpublish, privater zentraler PDF-Upload, Picker, Usage-Synchronisation und Asset-erhaltende Löschung funktionieren. Es existiert keine zweite Upload-/Dateiverwaltung. Details: [`b15-22a-download-module-analysis.md`](b15-22a-download-module-analysis.md), [`b15-22a1-download-module-live-design.md`](b15-22a1-download-module-live-design.md) und [`../modules/downloads.md`](../modules/downloads.md).
- Optionaler späterer Komfortpunkt, kein V1-Pflichtblock: Ein bestehendes privates, unbenutztes `purpose=document`-PDF über eine künftig atomare Usage-/Purpose-Operation in `purpose=download` übernehmen. Keine race-anfällige SELECT/UPDATE-Lösung bauen; öffentliche PDFs weiterhin nicht direkt umklassifizieren.
- B15.22D abgeschlossen und manuell verifiziert: `/downloads` zeigt nur vollständig konsistente veröffentlichte Einträge in aktiven Kategorien. `GET /downloads/[id]/file` revalidiert Download, Kategorie, privates PDF-Asset und exakte Usage serverseitig und leitet erst dann auf eine 120 Sekunden gültige Signed URL weiter. Ablauf, Deaktivierung mit anschließendem 404 und Wiederveröffentlichung wurden real geprüft.
- B15.22E abgeschlossen und manuell verifiziert: Der bestehende Footer verlinkt unter „Verein“ intern auf `/downloads`; Seite und Dateiabruf funktionieren weiterhin. Damit ist B15.22A–E funktional abgeschlossen. Die finale manuelle Mobile-/Design-Abnahme wurde nicht durchgeführt und bleibt beim öffentlichen Website-Redesign; Versionierung und der optionale atomare Adoption-Pfad bleiben bis zu eigenen Anforderungsblöcken außen vor.

## Priorität 3 – B15.21D11 Notification Center UX (abgeschlossen)

- Checkbox je Notification, Einzel-/Mehrfachauswahl, Auswahlzahl, sichtbare Gesamtauswahl und bestätigtes Sammellöschen sind für Desktop und Mobile implementiert. Die kompakte Aktionsleiste enthält außerdem „Alle als gelesen markieren“; der redundante UI-Einstieg „Gelesene löschen“ ist entfernt, während der interne Bestandsvertrag erhalten bleibt.
- Der Typfilter zeigt nur tatsächlich geladene Typen mit den deutschen Labels der zentralen Preference-Registry; die Tabelle ergänzt darunter dezent den technischen Key und unbekannte Typen erhalten einen neutralen Fallback.
- „Alle auswählen“ bezeichnet ausschließlich alle aktuell geladenen und durch die aktiven Filter sichtbaren Notifications, niemals ungeprüft zukünftige paginierte Datenbankseiten; Filterwechsel leeren die Auswahl.
- Action, Service und Repository verwenden serverseitige Session-Ownership, maximal 250 normalisierte UUIDs sowie `id IN (...) AND recipient_user_id = userId`. Fremde oder fehlende IDs erzeugen kein Existenzleck.
- Die freigegebene FK-Semantik bleibt bewusst bestehen: Das Löschen einer Notification entfernt wegen `notification_deliveries.notification_id ... ON DELETE CASCADE` auch deren operative Delivery-Ledgerzeilen. Das append-only `notification_audit` bleibt unabhängig erhalten. Es war keine SQL-Änderung erforderlich. Details: [`b15-21d11-notification-center-bulk-delete-analysis.md`](b15-21d11-notification-center-bulk-delete-analysis.md).
- Der manuelle Browsertest für Desktop, Mobile, Einzel-/Mehrfachauswahl, Auswahlzähler, Filter, Read/Unread, Sammellöschung, kompakte Aktionsleiste und deutsche Typbezeichnungen ist erfolgreich abgeschlossen.
- optional danach echte Pagination, Realtime/Tab-Synchronisierung, Retry-System und Audit-Aufbewahrung.

## Priorität 4 – Benutzer und Profile

- B15.23A – Bestandsanalyse abgeschlossen. Das bestehende Auth-/Adminprofil-, Rollen-, Trainer-, Vorstands- und Kontaktmodell bleibt verbindlich bestehen; eine neue `persons`-Tabelle oder umfassende Personenmigration ist nicht geplant.
- B15.23B – Security-Analyse bestehender Trainer-/Vorstands-/Kontaktstrukturen abgeschlossen; der Read-only-Live-Preflight bestätigte die historischen direkten Browser-Schreibrechte.
- B15.23B1 – Security-Hardening für `coaches`, `board_members` und `club_contacts` live umgesetzt. RLS/Grants/Policies und service_role-only RPCs wurden per Postcheck bestätigt; Superadmin-, berechtigte Rollen-, Public-, Kontakt-, Vorstand- und Media-Browsertests bestanden. Trainer bleiben im normalen Fachworkflow archiviert statt physisch gelöscht. Public-Read-Spaltenminimierung bleibt ein separater späterer Prüfpunkt.
- B15.23C/C1–C4 – vollständig abgeschlossen. SQL/Postcheck, Dashboardprofil mit Nickname/Telefon/privatem Avatar, read-only Stammdaten, kompakter Rollen-/Passwortbereich, SSR-PKCE-Recovery und deutsche Auth-Mail-Rate-Limit-UX sind live beziehungsweise manuell bestätigt. Der finale Recovery-Test einschließlich Passwortspeicherung und anschließendem Login bestand; Trainer-/Vorstandsbilder, Superadmin-Verwaltung und Rollenmatrix blieben unverändert.
- B15.23D – vollständig abgeschlossen. Der bestehende Admin-Einladungsflow wurde live Ende-zu-Ende mit Primärrolle Fußball-Vorstand und zusätzlicher Rolle Trainer verifiziert: Benutzeranlage, Einladung über Supabase Auth Custom SMTP/Resend, externe Zustellung, `/admin/set-password`, Passwortvergabe, Login, Session, Dashboard und Rollen-/Permission-Auswertung bestanden ohne Code-, SQL- oder Datenbankkorrektur. `mail.mavermg.de` sowie DKIM und Sending-CNAMEs sind verifiziert. Das derzeit englische Supabase-Invite-Template bleibt ein späterer separater Mailtemplate-/Corporate-Design-Punkt; die finale SMTP-/Mailserver-Umstellung bleibt Go-live-Aufgabe.
- Bestehende explizite Trainer-/Vorstandslinks zum Adminprofil erhalten; keine Zuordnung oder Autorisierung über E-Mail-/Namensabgleich ausbauen.
- B15.23E – **COMPLETE – LIVE CORE FLOWS VERIFIED.** Der enge `auth.users`-Guard ist live. Self-Service-Block, Passwort, Login/Logout/Session, vollständige Recovery, Invite und E3-Forward wurden live erfolgreich bestätigt. Der finale manuelle Read-only-DB-Postcheck ist vollständig **PASS** (8/8 Abschlussprüfungen wahr, keine Pending-Zustände, keine aktiven Requests und keine Auth/Profile-Inkonsistenzen). Der zusätzliche künstliche Compensation-Livetest wird bewusst nicht ausgeführt: **COMPENSATION LIVE FAILURE INJECTION NOT EXECUTED / COMPENSATION COVERED BY AUTOMATED TESTS**. Dies ist kein Abschlussblocker. Der Guard im Supabase-managed Auth-Schema bleibt upgrade-sensitiv; nach relevanten Supabase-/GoTrue-Upgrades sind Guard-, Auth- und Compensation-Regressionen verpflichtend.
- später Rollen für weitere Abteilungen.

## Priorität 5 – B15.24 Öffentliche Website (aktiv)

- B15.24A abgeschlossen: Dokumentationskonsolidierung und vollständige Code-IST-Analyse inventarisieren 30 öffentliche Seiten, zwei öffentliche Handler, Navigation, Header, Footer, Designbasis, Responsive-Risiken, Inhalte, Datenquellen, Accessibility, Performance, SEO und rechtliche Go-live-Flächen. Details: [`b15-24a-public-website-inventory.md`](b15-24a-public-website-inventory.md).
- Die Website wird als Auftritt des Gesamtvereins mit den gleichrangigen Abteilungen Fußball, Tischtennis, Behindertensport und Gymnastikdamen weiterentwickelt. Die iterative Betreiberentscheidung und die bestandene Desktop-/Smartphone-Abnahme aus B15.24B ersetzen den ursprünglich offenen externen Referenzinput; die bestätigte Dunkel-/Rot-/Weiß-Designbasis ist verbindlich.
- B15.24B – **COMPLETE – MANUAL DESKTOP AND RESPONSIVE REVIEW PASSED.** Gesamtvereins-Informationsarchitektur, gemeinsame öffentliche Designbasis, zugängliche Desktop- und vollständige Mobilnavigation, Floating-Navigation, Branding/Header, Footer, Hero-/Card-System und öffentlicher Rot-/Anthrazit-Hintergrund sind umgesetzt. Die dynamische Homepage verbindet News mit den mannschaftsübergreifend global sortierten Top-5-Trainingsterminen; Startseite, Featured-NewsCard, Terminbereich, Vereinsgeschichte, History-Encoding, Membership-Datumsfeld und mobiler Header wurden responsiv korrigiert. Der abschließende reale Desktop-, Smartphone- und Responsive-Review einschließlich Navigation sowie Social-/Standorticons ist bestanden.
- B15.24C – **COMPLETE – AS-BUILT DESIGN BASIS CONSOLIDATED.** Der bestätigte Ist-Stand ist in der bestehenden Website-Moduldokumentation als verbindlicher Vertrag für Farben/Flächen, Typografie, Layout, Karten, Responsive und Interaktion dokumentiert. Identische öffentliche Hauptgründe verwenden das bestehende `--dunkel`-Token; es wurde keine zweite Tokenwelt und kein Redesign eingeführt. Eine externe Referenzwebsite ist für diese Designbasis nicht mehr erforderlich.
- B15.24D – **COMPLETE – HEADER AND GLOBAL LAYOUT CONTRACT ALREADY DELIVERED BY B15.24B/C.** Der formale Soll-Ist-Abgleich bestätigt den festen responsiven Header, die Floating Desktop Navigation, den Mobile Header, die zentrale Root-/Seitenoffset-Geometrie, den 90-rem-Chrome-Rahmen sowie `PublicPageShell`, `PublicPageHero` und `PublicCard`. Es ist keine zusätzliche Produktarbeit erforderlich; Desktop und 360–430-Pixel-Darstellung wurden bereits in B15.24B manuell abgenommen und in B15.24C als Designvertrag festgehalten.
- B15.24E – **COMPLETE – PUBLIC FOOTER ACCEPTED AS DELIVERED.** Gesamtvereinsfooter, Kontakt-/Rechtlich-Hierarchie und ausschließlich tatsächlich vorhandene Links wurden durch B15.24B umgesetzt und im abschließenden Desktop-/Mobile-Review vom Betreiber freigegeben. Es werden keine fehlenden AGB-, Register- oder externen Links erfunden.
- **PRE-GO-LIVE LEGAL CMS / FOOTER – TECHNICAL AND MANUAL REVIEW COMPLETE.** Impressum, Datenschutz und AGB/Nutzungsbedingungen sind im bestehenden CMS angelegt, veröffentlicht und vom Betreiber auf Desktop und Mobile geprüft; `/agb` ist erreichbar. Der gemeinsame Richtext-Vertrag speichert neue Umlaute, Sonderzeichen und typografische Zeichen Unicode-stabil und normalisiert bestehende HTML-Entities im weiterhin allowlist-sanitisierten Public-Renderer ohne Double-Encoding oder XSS-Aufweichung. Der Footer lädt generisch ausschließlich `is_published = true` plus `show_in_footer = true`, sortiert nach `sort_order` mit deterministischem Titel-/Slug-Tie-Breaker und hält Cookie-Einstellungen als getrennte Consent-Aktion. Alle Footer-Textlinks verwenden den gemeinsamen Vereinsrot-Hover und sichtbaren roten `focus-visible`-Vertrag; der manuelle Footer-, Consent- und Legal-Review ist bestanden.
- B15.24F – **COMPLETE – PUBLIC CLUB PAGES ACCEPTED AS DELIVERED.** Die visuelle Vereinheitlichung der allgemeinen öffentlichen Vereinsseiten wurde durch B15.24B umgesetzt und vom Betreiber freigegeben. Eigenständige Formular-, Inhalts- oder Integrationsarbeit bleibt von diesem Designabschluss unberührt.
- B15.24K – **COMPLETE – MANUAL PUBLIC REVIEW PASSED.** `/verein/vorstand` zeigt über einen server-only, fail-closed Public-Loader ausschließlich aktive Gesamtvereinsmitglieder mit `organization_scope = club` und `department_id IS NULL`, geordnet nach der gespeicherten Reihenfolge. Das minimale Public-DTO enthält Name, Funktion und ein über den bestehenden Public-Media-Vertrag aufgelöstes Bild beziehungsweise den zentralen Personen-Placeholder; mangels explizitem Freigabevertrag werden keine E-Mail-/Telefonwerte ausgegeben. Football, Tischtennis, `unassigned` und inaktive Einträge bleiben ausgeschlossen. Dashboard, Public-Seite, Scope-Trennung, Deaktivierung/Reaktivierung, Sortierung, Bilder/Placeholder sowie Desktop-, Tablet- und Mobile-Darstellung sind im Betreiberreview bestanden. Die Responsive-/Mobile-Abnahme der allgemeinen Websitebasis gehört weiterhin zum abgeschlossenen B15.24B-Vertrag.
- B15.24M – **COMPLETE – MANUAL CONSENT REVIEW PASSED.** Der versionsgebundene, fail-closed Public-Consent-Manager unterscheidet ausschließlich technisch notwendige Funktionen und optionale externe Inhalte. FUSSBALL.DE sowie ein gegebenenfalls konfiguriertes Google-Maps-Embed bleiben bis zur Zustimmung technisch ungeladen; click-TT bleibt als server-only Datenfluss ungeblockt. Footer und `/cookie-einstellungen` öffnen dieselben speicher-, änder- und widerrufbaren Einstellungen. First Visit, Auswahlwege, Persistenz, Network-/Storage-Vertrag, Widerruf, Tastatur, Fokus, Mobile und 200-%-Zoom sind durch den Betreiber bestätigt. **Impressum und vollständige Datenschutzerklärung bleiben zwingende Pre-Go-live-Gates.** Details: [`b15-24m-cookie-consent-dsgvo-contract.md`](b15-24m-cookie-consent-dsgvo-contract.md).
- **LEGAL CONTENT GATE – CONTENT PRESENT, FINAL PROVIDER REVIEW OPEN.** Die drei CMS-Rechtsseiten sind technisch vorhanden und manuell geprüft. Vor Produktivfreigabe bleiben dennoch die finale Ergänzung beziehungsweise Bestätigung des Hostinganbieters, die Prüfung der technischen Datenschutzangaben und des Mail-/SMTP-Anbieters, ein abschließendes Inventar aller externen Dienste/Einbindungen, gegebenenfalls AV-Verträge und Drittlandtransfers sowie der finale juristische Legal-Review offen. Dieser technische Abschluss ist keine juristische Freigabe.
- B15.24G – **COMPLETE – CURRENT FOOTBALL AREA ACCEPTED AS DELIVERED.** Bestehende Mannschaftsseiten, Kader innerhalb der Mannschaftsseite, Trainer/Ansprechpartner und Trainingszeiten bleiben unverändert. Eigenständige Spielerprofile sind nicht vorgesehen; eine separate Fußballchronik ist nach Betreiberentscheidung nicht erforderlich, die Gesamtvereinsgeschichte genügt. Der bestehende Legacy-Redirect bleibt erhalten.
- **GO-LIVE – FUSSBALL.DE-Widgets:** Spielpläne, Ergebnisse und Tabellen erst nach Bereitstellung der finalen registrierten Produktivdomain konfigurieren, einbinden beziehungsweise abschließend verifizieren. Dann Domainregistrierung/-freigabe, Mannschaftszuordnung sowie Desktop- und Mobildarstellung prüfen. Status: **BLOCKED UNTIL FINAL LIVE DOMAIN**; der temporäre Cloudflare-Zugang reicht für diese Prüfung nicht aus.
- **FUTURE / POST-GO-LIVE – Turniere & Events:** Den vorhandenen Aufbauzustand unverändert lassen und den Bereich mit Turnierübersichten, Details, eventuellen Anmeldungen und weiteren Funktionen erst in einem späteren eigenständigen Block vollständig ausbauen.
- B15.24H1 – **COMPLETE – TABLE TENNIS DB FOUNDATION AND SECURITY HARDENING VERIFIED.** Live-Preflight, manuell ausgeführtes DB-Proposal, vollständiger Read-only-Postcheck und der anschließende Security-Korrektur-Postcheck sind bestanden. Tischtennis-Abteilung, Rolle, Permissions, Department-Scopes, RLS, sichere Active-only-Public-Reads und die auf `authenticated`/`service_role` begrenzten H1-Helper sind live bestätigt. Die lokalen Diagnose-CSV-Dateien bleiben unversioniert.
- B15.24H2 – **COMPLETE.** Das Tischtennis-Dashboard, die zentrale Department-Struktur und der finale Rollen-/Permission-Vertrag sind technisch und manuell abgenommen. Explizite `/admin/football/*`- und `/admin/table-tennis/*`-Fachrouten stehen neben neutralen globalen Inventaren. `players.department_id`, `coaches.department_id` und `teams.department_id` sind die organisatorischen Department-Master; Mannschaften bleiben davon getrennte saisonale Relationen und dürfen bei Player/Coach fehlen. Teams mit `department_id = NULL` sowie Board-Einträge mit `organization_scope = unassigned` bleiben nicht zugeordnet. Board verwendet den konsistenten Vertrag `unassigned | club | department` und ist fachlich in Gesamtverein, Fußball und Tischtennis getrennt. Legacy-Relationskonflikte werden nicht automatisch migriert, sondern nur im Superadmin-Structure-Center sichtbar und kontrolliert auflösbar. Team-/Contribution-Zähler berücksichtigen ausschließlich aktive department-kompatible Relationen. Tischtennis verwendet `strong_hand`, TT-Lizenzen, TT-Trainingsarten und Halle; Football-Position/Rückennummer erscheinen nicht im TT-Detail. Öffentliche Football-Abfragen bleiben fail-closed auf das Football-Department. Structure Center ist Superadmin-only, Cross-Department-Zugriffe werden abgewiesen und privilegierte Writes bleiben server-only. Player-, Coach-, Team-, Training-, Board- und Role-Smoke-Browserreviews sind PASS. Ein Board-Unassigned-Claim war mangels Testdatensatz nicht manuell ausführbar, ist aber automatisiert abgedeckt. Keine H2-Funktionsblocker und kein weiterer DB-Gate sind offen.
- **H2 SQL-As-built:** Die Masterdata-, Review-Data-Contract-, Central-Structure-Assignment- und Role-Contract-Proposals wurden während H2 manuell ausgeführt und per Read-only-Postchecks bestätigt. Preflights, ACL-Fingerprint-, Round-2-Diagnose- und Postcheck-Dateien sind ausschließlich lesende Diagnoseartefakte. Rollbacks wurden nicht ausgeführt und bleiben klar gekennzeichnete Sicherheitsartefakte. Die verifizierten DB-Änderungen gehören zum As-built-Stand; in diesem Abschlusslauf wurde kein SQL ausgeführt.
- **H2 As-built-Permission-Matrix:** Globales Inventar: Superadmin und Vorstand global; Rollen mit bestätigtem Read-only-/Beitragsvertrag nur bei vorhandener View-Permission lesend, Department-Vorstände ausschließlich eigenes Department, Trainer/Betreuer ausschließlich eigene Teams. Struktur & Zuordnung: nur Superadmin. Football/TT Players, Coaches, Teams und Training: Superadmin global, jeweiliger Department-Vorstand nur eigenes Department, fremder Department-Vorstand deny, Trainer/Betreuer nur bestehende eigene Teams. Boards: Superadmin alle inklusive `unassigned`; Vorstand Club und Departments; Department-Vorstände nur eigene per `admin_profile_id` verknüpfte Kachel; Kassierer, Webmaster und Trainer/Betreuer deny. Contributions bleiben beim bestehenden Vertrag (Superadmin vollständig, Vorstand View/Export, Kassierer beitragsbezogen); Media/Content bleibt unverändert beim bestehenden Permissionvertrag, insbesondere zentrale Mediennavigation nur Superadmin/Webmaster.
- **Role-Contract Browser-Checkliste:** Superadmin: vollständiges Board, System, vollständige Contributions und Fachbereiche. Vorstand: Fußball/Tischtennis und Board für Club/Departments, kein System, Contributions nur View/Export. Fußball-/Tischtennis-Vorstand: nur eigenes Departmentmanagement, Board ausschließlich eigene verknüpfte Kachel, kein Create/Delete und kein System. Kassierer: Contribution-Vertrag, kein Board/System allein durch die Rolle. Webmaster: kein Board/System allein durch die Rolle. Da derzeit alle sechs Board-Einträge `admin_profile_id = NULL` haben, ist eine leere Own-Card-Ansicht für Department-Vorstände erwartungsgemäß korrekt.
- B15.24H3 – **COMPLETE – PUBLIC TABLE TENNIS WEBSITE INTEGRATION VERIFIED.** Der abschließende Betreiberreview ist vollständig bestanden. H3.1 und H3.2 einschließlich F1–F6 bilden den finalen öffentlichen Tischtennisvertrag; für H3 waren weder DB-Änderung noch SQL-Ausführung erforderlich.
- B15.24H3.1 – **COMPLETE / VERIFIED.** Der `server-only` Public-Tischtennis-Datenvertrag löst das aktive Department ausschließlich über den stabilen Slug `tischtennis` auf und liefert fail-closed normalisierte DTOs für aktuelle Mannschaften, Mannschaftsdetail, Saison, strukturierte Trainingszeiten, kompatiblen TT-Kader, TT-Trainer, Department-Vorstand, öffentliche Medien, explizite Teamkontakte und den neutralen Spielbetriebsstatus `external_integration_deferred`. Football-, `NULL`-, unassigned-, inaktive und inkonsistente Datensätze werden ausgeschlossen; zentrale nicht öffentliche Medien fallen nicht auf Legacy-URLs zurück.
- B15.24H3.2 – **COMPLETE / MANUAL BROWSER REVIEW PASSED.** Landingpage, datengetriebene Mannschaftsübersicht, Mannschaftsdetailroute, Trainingszeiten und TT-Vorstand verwenden ausschließlich den H3.1-Vertrag. Das Detail bietet Training als Standardtab sowie Kader, Trainer, Spielbetrieb und Kontakt als einzeln geöffnete Tabs. Sichere Detail-Metadaten, responsive TT-Karten und fail-closed 404 für unbekannte oder fachfremde Slugs sind bestätigt. Spielplan/Tabelle bleibt eine nicht integrierte externe Spielbetriebsintegration; es werden keine Spiele oder Tabellen simuliert.
- B15.24H3.2F1 – **COMPLETE / MANUAL REVIEW PASSED.** Der Browserblocker durch die nicht existente Live-Spalte `seasons.name_de` ist im Public-Loader auf den bestätigten Saisonvertrag mit `seasons.name` korrigiert. Optionale Detail-/Mediafehler entfernen keine gültigen Mannschaften oder Personen mehr; TT-Spieler, -Trainer und -Vorstände verwenden ohne eigenes Public-Bild den bestehenden zentralen Personen-Placeholder. RLS, DB und Departmentfilter blieben unverändert.
- B15.24H3.2F2 – **COMPLETE / MANUAL BROWSER REVIEW PASSED.** URL-kodierte bestehende DB-Slugs werden am Detail-Loader einmalig und fail-closed normalisiert; der reale Herren-Link lädt damit im frisch gestarteten Server, während unbekannte und fachfremde Slugs 404 bleiben. Football und Tischtennis verwenden dieselbe responsive, zugängliche Einzelauswahl für Training, Kader, Trainer, Spielbetrieb und Kontakt. Das TT-Detail zeigt `Tischtennisabteilung`, Football behält `Fußballabteilung`.
- B15.24H3.2F3 – **COMPLETE / MANUAL CONTACT REVIEW PASSED.** Der finale öffentliche TT-Teamkontaktvertrag lautet **EXPLICIT TEAM CONTACT ONLY**. Ohne explizit für die Mannschaft hinterlegte, öffentlich freigegebene Kontaktdaten liefert der Serververtrag `contact = null`; weder Trainer noch TT-Vorstand werden als Kontaktfallback verwendet. Der Kontakt-Tab zeigt dann den bestätigten deutschen Leerzustand. Trainer-Tab und eigenständige TT-Vorstandsseite bleiben unverändert; die im Teamdetail nur für den früheren Fallback benötigte Board-Abfrage entfällt.
- B15.24H3.2F4 – **COMPLETE / DIAGNOSIS SUPERSEDED BY F5.** Listen- und Detailloader verwenden denselben fail-closed Bildresolver; der damalige Betreiber-Smoke widerlegte eine reine Altprozessursache und führte zum vollständigen DTO-/Merge-Trace in F5. Der daraus nachgewiesene Verlustpunkt ist behoben und im finalen Bildreview bestätigt.
- B15.24H3.2F5 – **COMPLETE / MANUAL IMAGE REVIEW PASSED.** Der tatsächliche Verlustpunkt lag im Summary-Merge: `loadPublicTableTennisTeamSummaries` lud die Detail-DTOs bereits, übernahm daraus aber nur Training und Kontakt. Eine dort korrekt aufgelöste `team.imageUrl` ging verloren, wenn die vorherige Listenauflösung `null` geliefert hatte. Der Merge übernimmt nun vorrangig die bereits öffentlich und fail-closed aufgelöste Detailbild-URL und behält andernfalls die Listen-URL beziehungsweise `null`. Das reale Bild der 1. Herren ist in Liste und Detail bestätigt; Mannschaften ohne öffentliches Bild verwenden den vorgesehenen Placeholder.
- B15.24H3.2F6 – **COMPLETE / RUNTIME REVIEW PASSED.** Die datenbankabhängige Mannschaftsübersicht wartet über die stabile Next-16-API `connection()` auf einen Request und wird in Development und Production Preview zur Laufzeit aufgelöst, statt Build-Daten einzufrieren. Nach sauberem Neustart liefern localhost und Cloudflare Quick Tunnel denselben aktuellen Stand. TT-Media-, DTO-, Card-, Privacy- und Kontaktlogik bleiben unverändert; es existiert keine tunnel- oder localhost-spezifische Produktlogik.
- Nacharbeit 11 ergänzt oberhalb der Floating-Navigation eine kompakte Desktop-Serviceleiste mit konfiguriertem Instagram-Link sowie den bestehenden Zielen `/kontakt` und `/termine`. Der Footer ist auf fünf responsive Informationsbereiche umgestellt und trennt Vereinspräsentation, Verein, Sportarten, weitere interne Links und zentrale `club_settings`-Kontaktdaten. Impressum/Datenschutz erscheinen nur publiziert; AGB und Cookie-Einstellungen bleiben mangels realer Route beziehungsweise Consent-Verwaltung bewusst ohne UI. Verifizierte externe Verbands-/Ortslinks fehlen weiterhin als Contentpunkt. NewsCards blieben unverändert. Der damals noch ausstehende Gesamtreview ist inzwischen bestanden.
- Nacharbeit 12 vereinheitlicht Header und Footer auf den gemeinsamen validierenden Resolver für `club_settings.social_links` (`facebook`, `instagram`, `youtube`, `tiktok`, `linkedin`, `x`) und blendet ausschließlich gültig gepflegte Ziele ein. `/anfahrt` zeigt die zentrale Vereinsadresse und behandelt `club_settings.google_maps_url` defensiv: normale gültige Maps-Links werden extern angeboten, eine zulässige Embed-URL erst nach bewusstem Klick geladen; eine persistente Consent-Steuerung bleibt offen. `/cookie-einstellungen` ist eine transparente Aufbau-Route ohne Fake-Controls und im Footer verlinkt; Powered-by wurde entfernt. AGB, Vereinsregister-Inhaltsprüfung, verifizierte Verbands-/Ortslinks und die reale Consent-Verwaltung bleiben offen. Der damalige manuelle Review ist inzwischen bestanden.
- Nacharbeit 13 stellt dieselben dynamischen Social Icons in Header und Footer mit plattformtypischen Markenfarben dar. Für `/anfahrt` ist die offizielle Google Maps Embed API über den ausschließlich serverseitig gelesenen Konfigurationsnamen `GOOGLE_MAPS_EMBED_API_KEY` vorbereitet; der Key fehlt derzeit und darf nicht committed werden. Die zentrale Adresse bildet die Suchanfrage, der vorhandene normale `google_maps_url` bleibt Fallback. Ohne gültige Konfiguration entsteht kein iframe; mit Konfiguration wird Google erst nach bewusstem Sitzungsklick geladen. Die spätere Anbindung an die echte Consent-Steuerung bleibt offen.
- Nacharbeit 14 verwirft die originalfarbigen Brandflächen nach manuellem Designreview. Alle dynamischen Social Glyphs verwenden nun wie Standort und Kalender transparenten Hintergrund sowie die vorhandenen Website-Tokens `text-red-500`/`hover:text-red-400`; Resolver und Accessibility bleiben unverändert.
- Nacharbeit 15 setzt diese Farbtokens direkt auf das jeweilige SVG-Glyph. Dadurch kann die globale Linkregel `a { color: inherit; }` den Normalzustand nicht mehr auf Weiß zurücksetzen; Header und Footer bleiben geometrisch und funktional unverändert.
- **OFFEN – Google Maps Inline/Embed:** Google-Cloud-Projekt einrichten, Maps Embed API aktivieren, geeigneten Key erstellen und auf die benötigte API sowie zulässige Website-Referrer/Produktivdomain beschränken. `GOOGLE_MAPS_EMBED_API_KEY` ausschließlich serverseitig konfigurieren und niemals committen. Danach Inline-Karte auf `/anfahrt` mit der zentralen Vereinsadresse aktivieren, normalen `google_maps_url` als externen Fallback erhalten und unter dem in B15.24M vorhandenen Consent-Gate im Go-live-Test prüfen.
- **IMMEDIATELY BEFORE GO-LIVE / LEGAL CONTENT GATE:** Testdaten bereinigen, Echtdaten einpflegen, Impressum und vollständige Datenschutzerklärung einpflegen, Betreiber-/Vertretungs-/Hosting-/Domain-/Kontaktangaben sowie Rechtsgrundlagen und anbieterbezogene Pflichtinformationen prüfen, Consent-Inventar gegen den finalen Deploymentstand abgleichen und beide Rechtstexte manuell gegenlesen. Google-Maps-Go-live bleibt zusätzlich von API-Key, Domainbeschränkung und realer Embed-Konfiguration abhängig.
- Nacharbeit 16 führt eine gemeinsame öffentliche Designbasis ein: Root-Verlauf für sämtliche Website-Routen, zentraler responsiver Abstand unter Header/Floating-Navigation sowie `PublicPageShell`, `PublicPageHero` und `PublicCard`. Vereinsübersicht, Gesamtvorstand/-geschichte, Behindertensport, Gymnastikdamen, Tischtennis, Termine/Training, Downloads, Fußballübersicht, Mannschaftsübersichten, Anfahrt und Rechtseiten verwenden die gemeinsame Familie; unnötige äußere Seitenkarten wurden in den normalen Übersichten entfernt. Sonderlayouts behalten ihre Fachstruktur auf demselben Hintergrund und Offset. Der systematische visuelle Browserreview ist abgeschlossen.
- Nacharbeit 17 bereinigt die Informationsarchitektur: Verein- und Header-Termine führen direkt nach `/termine/allgemein`, Fußball enthält keinen allgemeinen Termine-Punkt, Tischtennis verwendet die neue Route `/tischtennis/trainingszeiten`, und News führt ohne doppelten Navigationspunkt kanonisch nach `/news/uebersicht`. `/termine` bleibt als kompatible neutrale Übersichtsroute erhalten. Die erneute Repository- und Git-Historienanalyse bestätigt `fussball-vereinsgeschichte` als 2026 eingeführten technischen Legacy-Singleton-Key: Der einzige Editor liegt heute unter Gesamtverein und verwaltet fachlich die Vereinsgeschichte. `/verein/vereinsgeschichte` zeigt daher die veröffentlichten realen CMS-Inhalte; `/fussball/vereinsgeschichte` bleibt als Legacy-URL mit Redirect erhalten. Eine semantische DB-Key-Migration ist optionaler separater Datenbankblock und wird nicht in B15.24B durchgeführt.
- Nacharbeit 18 verkleinert die Featured-NewsCard ausschließlich auf Smartphones und stellt ihre bisherigen Abmessungen ab `sm`/Desktop wieder her. Eine History-lokale, einmalige Entity-Allowlist normalisiert Legacy-Umlaute und `&amp;`, bevor der bestehende Richtext-Sanitizer greift; unbekannte Entities und HTML bleiben unter dessen Schutz. Der native Geburtsdatum-Indikator erhält nur mobil etwas rechten Innenabstand. Diese Punkte sind im abschließenden Mobile-/Responsive-Review bestätigt.
- Nacharbeit 19 behebt die tatsächliche Homepage-Überbreite: Der nicht umbrechende Kopf der Termin-Sidebar vergrößerte als Grid-Kind die gemeinsame mobile Min-Content-Spalte und damit auch die NewsCard. Die Startseite erzwingt nun bis `lg` einen schrumpfbaren `minmax(0,1fr)`-Track; NewsCard und Terminbox besitzen durchgängige `min-w-0`-/Breitenverträge. Der mobile Header zeigt dynamisch nur konfigurierte Instagram-/Facebook-Icons sowie den Anfahrt-Pin vor dem unveränderten Hamburger; der Termine-Service bleibt Desktop vorbehalten. Der reale Mobile-/Responsive-Review ist bestanden.
- Nacharbeit 20 vereinheitlicht die mobile Serviceicon-Gruppe mit identischen 40-Pixel-Touchflächen, einem gemeinsamen strukturellen Abstand und optisch ausgeglichenen Instagram-, Facebook- und Standortglyphen. Der Hamburger bleibt separat und funktional unverändert. Der reale Smartphone-Review bei kleinen Viewports ist bestanden.
- B15.24K2 – **COMPLETE – DB FOUNDATION, PROVIDER, DASHBOARD AND PUBLIC MULTI-TEAM REVIEW PASSED.** DB-Preflight, manuell ausgeführtes Proposal und Read-only-Postcheck sind bestanden. Dashboardkonfiguration, getrennte saisonale Zuordnung beider TT-Teams, `/tischtennis/spielplan-tabelle`, gemeinsamer Tabellen-/Spielplanwechsel, Teamdetail-Integration und Responsive-Vertrag wurden vom Betreiber bestätigt. Feste Host-Allowlist, server-only Adapter, 15-Minuten-Revalidation, externe Team-ID-Filterung, Privacy-Grenze und Football-Isolation bleiben Bestandteil des As-built-Vertrags. Aktuelle Zuordnungen sind Testdaten und vor Go-live saisonbezogen zu prüfen. Details: [`b15-24k2-mytischtennis-integration-preflight.md`](b15-24k2-mytischtennis-integration-preflight.md).
- B15.24K3.1 – **COMPLETE – FUSSBALL.DE WIDGET MULTI-TEAM INTEGRATION MANUALLY VERIFIED.** Widget Save, Replace, Remove und Persistenz nach Reload sind für Spielplan und Tabelle bestanden. Der bereinigte Script-/DOM-Lifecycle verhindert doppelte beziehungsweise zurückbleibende Widgets. `/fussball/spielplan-tabelle` bietet eine datengetriebene Mannschaftsauswahl; Tabelle und Spielplan wechseln gemeinsam und bleiben cross-team isoliert. Mannschaftsdetail-Integration und Responsive-Verhalten sind ebenfalls manuell bestätigt. Die offizielle Domainbindung bleibt unverändert.
- B15.24K3.2 – **COMPLETE – MANUAL VISUAL REVIEW PASSED.** Der gemeinsame Wrapper für zentrale Seite und Teamdetail besitzt keine eigene weiße Innenkarte und kein weißes Padding mehr; dunkler Übergang sowie auf 100 Prozent begrenzte Root-/iframe-Flächen sind manuell bestätigt. F1 entfernt den versetzt wirkenden roten Innenrahmen und eigenen Frame-Radius; die äußere Website-Karte bleibt die einzige sichtbare Begrenzung. FUSSBALL.DE-Header, Dropdown, native Options, interne Scrollfläche und weißer unterer Tabellenbalken liegen akzeptiert im cross-origin-iframe und werden nicht lokal manipuliert. Anbieterfarben bleiben ausschließlich im FUSSBALL.DE-Widgetcenter gepflegt.
- **POST-GO-LIVE – FUSSBALL.DE AJAX-/XHR-Provider-Test:** Erst nach Deployment auf der echten beziehungsweise produktionsnahen Domain, Website-Freigabe und Verfügbarkeit einer separaten Testumgebung die Endpunkte `ajax.team.matchplan`, `ajax.team.next.games`, `ajax.team.prev.games` und `ajax.team.table` über mehrere Mannschaften, Altersklassen, Staffeln und Saisonwechsel untersuchen. Team-, Staffel- und Match-IDs, Stabilität, Cache, Rate Limits, Fehlerverhalten und einen server-only Provider-DTO-Vertrag bewerten. Nur bei belastbarer Stabilität einen austauschbaren `FootballDeProvider` analog click-TT und eine eigene UI prüfen; andernfalls offizielle Widgets dauerhaft verwenden. Die bestehende Widgetlösung bleibt in jedem Fall Fallback.
- **Football.de-Providerresearch eingeordnet:** Der Betreiber hat die vorhandene offizielle Widgetintegration als Zielvertrag bestätigt. Eine neue Ajax-/Providerimplementierung ist nicht Bestandteil von K3.1; der bestehende Widgetvertrag wird lifecycle-sicher und mehrmannschaftsfähig weitergeführt.
- B15.24K4 – **COMPLETE – MANUAL PUBLIC SCOPE AND TRAINING REVIEW PASSED.** Football-Trainer, Football-Vorstand und Football-Mannschaften sind strikt auf das aktive Football-Department begrenzt; Club Board, Tischtennis und unassigned Datensätze bleiben getrennt beziehungsweise öffentlich ausgeschlossen. `/termine/training` bleibt global und gemischt, `/fussball/trainingszeiten` ist Football-only und `/tischtennis/trainingszeiten` TT-only. Der gemeinsame Occurrence-Vertrag unterstützt „Heute & Morgen“ und die rollierende Wochenansicht. Trainingsdetails lösen Team- oder Bereichslinks ausschließlich aus relationaler Ownership auf; sichere Legacy-Pfadsegmente werden kodiert, unsichere oder unbekannte Ziele bleiben fail-closed. Maps verwendet den tatsächlichen Trainingsort. Der vollständige Betreiberreview ist bestanden.
- B15.24K4F1 – **PASSED.** Die TT-Trainingsroute verwendet die gemeinsame Football-/Global-Occurrence-, Range-, Karten-, Detail- und Maps-UX bei strikt relationalem Tischtennis-Scope.
- B15.24K4F2 – **SUPERSEDED / INTERMEDIATE CORRECTION.** Das Entfernen der abweichenden Saisoninterpretation und der gemeinsame Range-Vertrag waren korrekte Teilkorrekturen, aber nicht die abschließende Ursache des Runtime-Verlusts.
- B15.24K4F3 – **PASSED.** Finale Root Cause war die unzulässige Kopplung der Department-Sichtbarkeit an die URL-Validierung des Legacy-Team-Slugs `1 herren`. Die Department-Auswahl basiert ausschließlich auf `source_type`, `team_id` und relationalem `department_slug`; Linkfähigkeit wird separat validiert und kodiert. Der reale TT-Termin, Detail, Maps und TT-Mannschaftslink sowie Football-/Department-Isolation sind manuell bestätigt. DB und Testdaten blieben unverändert.
- B15.24L – **COMPLETE – MANUAL QUALITY REVIEW PASSED.** Skip-Link/Main-Fokus, Reduced Motion, Tastaturbedienung, sichtbare Fokuszustände, Desktop-/Mobilnavigation, Formbedienung und 200-%-Zoom sind durch den Betreiber bestätigt; keine akute Accessibility-Blockade wurde festgestellt. Der reale Browserreview war auch für TT-Training unauffällig. Titel-/OpenGraph-/Twitter-Fallback, Admin-/Auth-`noindex`, `robots.txt`, `sitemap.xml` und der fail-closed Stagingvertrag sind umgesetzt. Production-Indexierung erfordert bewusst `PUBLIC_SITE_INDEXING_ENABLED=true` plus valide HTTPS-`NEXT_PUBLIC_SITE_URL`. Individuelle Route-Metadata, Structured Data, eigene 404-UX und instrumentelle Kontrastmessung bleiben nicht blockierende Follow-ups. B15.24M und B15.24N sind abgeschlossen. Details: [`b15-24l-accessibility-performance-seo-audit.md`](b15-24l-accessibility-performance-seo-audit.md).
- B15.24I – **COMPLETE – DASHBOARD, PUBLIC SECTION, SECURITY CORRECTIONS AND HOMEPAGE TRAINING MANUALLY VERIFIED.** I.1 Contract/Live-Preflight, I.2/I.3 DB-Foundation/Security, I.4 Dashboard, I.5 Public Website mit bestandenen F3/F4-Korrekturen und I.6 Homepage-Integration sind abgeschlossen. `/behindertensport` liefert den freigegebenen neutralen Section-Vertrag; aktive und gültige Department-Zeiten gelangen ohne Kontakt-/Media-Abhängigkeit in den gemeinsamen, vor der Top-5-Grenze global sortierten Homepage-Stream. Die freigegebenen DB-Proposals wurden im Gesamtblock manuell angewendet und read-only verifiziert; kein Rollback wurde ausgeführt. Details: [`b15-24i-disabled-sports-contract-inventory.md`](b15-24i-disabled-sports-contract-inventory.md).
- B15.24J – **COMPLETE – MANUAL DASHBOARD, PUBLIC, HOMEPAGE, ROLE AND RESPONSIVE REVIEW PASSED.** Der bestehende Department-Slug `damen-gymnastik` ist über den gemeinsamen Section-Editor unter `/admin/gymnastikdamen`, die datengetriebene Public-Route `/damen-gymnastik` und den neutralen Homepage-Department-Training-Stream integriert. Wiederverwendet werden `department_sections`, `department_training_times`, Public-RPC/DTO/Layout, Media-Usage, Contact-Vertrag, Schließzeiten und der Merge vor dem Homepage-Limit. Superadmin und Gesamtvereinsvorstand dürfen über die bestehenden Section-Permissions bearbeiten; andere Rollen bleiben DENY. Dashboard-Persistenz, Public-404/Republish, Homepage-Sortierung und Rollenabgrenzung wurden manuell bestätigt. Keine DB-Migration, kein SQL und kein neues Permissionmodell waren erforderlich. Details: [`b15-24j-gymnastics-section-implementation.md`](b15-24j-gymnastics-section-implementation.md).
- **Go-live-Datenvertrag:** Die derzeitigen Inhalte einschließlich der Board-Einträge sind Testdaten. Vor dem Produktivstart werden sie kontrolliert vollständig bereinigt und anschließend durch geprüfte Echtdaten neu befüllt; aktuelle Namen, Anzahlen und Zuordnungen sind kein produktiver Sollbestand.
- `/downloads` ist funktional, im Footer verlinkt und in der abgenommenen optischen sowie mobilen Public-Basis enthalten. Künftige eigenständige Download-Funktionen bleiben separaten Anforderungsblöcken vorbehalten.

## Priorität 6 – Weitere Abteilungen

Reihenfolge gemäß den bestehenden B15.24-Hauptpunkten: Tischtennis (H, abgeschlossen), Behindertensport (I, abgeschlossen), Gymnastikdamen (J, abgeschlossen). J verwendet den neutralen I-Vertrag mit strikt getrennten Section-/Training-Daten.

## Priorität 7 – Datenbank-/Saison-Cleanup

- **PRIORITY 7 COMPLETE.** Core Cleanup, Public Post-Cleanup Repair, Auth Cleanup und Media-/Storage-Cleanup sind abgeschlossen. Der finale Read-only-Gesamtpostcheck ist in allen acht Bereichen bestanden (`final_sql_pass = true`); der Betreiber hat Superadmin-Login, `/admin/profile` ohne Avatar und den Public Smoke bestätigt. Alle 31 Core-Delete-Tabellen sowie Media Assets, Media Usages und Storage Objects sind leer. Erhalten sind 1 Keep-Superadmin, 13 Rollen, 64 Permissions, 249 Rollen-Permission-Zuordnungen, 4 Departments, die Seasons `2026/2027` und `2027/2028`, 25 eindeutige Notification-Audit-Zeilen, 5 Storage-Buckets und 0 Closure Periods. `membership_request_recipients` besteht als konfigurierbare Foundation mit fachlich zulässigem Bestand `0..n`; der Vor- und Nach-Cleanup-Count ist jeweils 0, daher liegt kein Datenverlust vor. Details: [`priority7-database-cleanup-contract.md`](priority7-database-cleanup-contract.md).
- **TT-Landingpage-Nachprüfung bestanden:** `/tischtennis` verwendete zwar den korrekten TT-Team-Loader, war aber zusätzlich mit einer 15-Minuten-Prerender-Frist gecacht. Die Landingpage ist nun request-dynamisch und zeigt bei leerem Team-/Team-Season-Bestand den neutralen Empty State; der Betreiber hat den Public-Review bestätigt.
- **Priority 7 Auth Cleanup: COMPLETE.** Der Live-Preflight bestätigte exakt einen relationalen Keep-Superadmin, fünf Delete Candidates, 13 CASCADE- und 19 SET-NULL-FKs ohne blockierende RESTRICT-/NO-ACTION-Beziehung. Alle fünf Kandidaten wurden einzeln und fail-closed über die serverseitige Supabase Admin API entfernt. Final bestehen 1 Auth-User, 1 Keep-Profil, die aktive Superadminbindung und die unveränderte Foundation. Der Profil-Cleanup-Trigger entfernte acht Avatar-Usages gelöschter Profile; 33 Assets und Storage blieben in dieser Stufe unangetastet. Kein direktes SQL auf `auth.users`, keine hardcodierte E-Mail oder UUID. Die nachfolgende Media-/Storage-Stufe und der Gesamtpostcheck sind ebenfalls abgeschlossen. Details: [`priority7-auth-cleanup-execution-plan.md`](priority7-auth-cleanup-execution-plan.md).
- **Priority 7 Media/Storage Cleanup: COMPLETE.** Nach vollständigem Read-only-Preflight und ausdrücklicher irreversibler Operatorfreigabe wurden manifestbasiert 11 Usages, 33 Assetobjekte, 33 Assetzeilen und 76 bestätigte Storage-Orphans sequenziell entfernt. Final sind Media Assets, Usages und Storage Objects jeweils 0; alle fünf Buckets und die Keep-Foundation blieben erhalten. Kein direktes Storage-SQL und keine Bucket-Löschung. Gesamtpostcheck und manuelle Abschluss-Gates sind bestanden. Details: [`priority7-media-storage-cleanup-execution-plan.md`](priority7-media-storage-cleanup-execution-plan.md).
- **Post-Cleanup-Reconfiguration Vereinslogo: COMPLETE / MANUAL REVIEW PASSED.** Public Header, Mobile Header, Footer, SEO-Metadaten und Dashboard-Header verwenden dauerhaft das statische Repository-Asset `/images/club-logo.png`; Public- und Dashboard-Branding funktionieren damit auch bei `media_assets = 0` und `storage.objects = 0`. Es war keine DB- oder Storage-Reparatur erforderlich. Der Betreiber hat Desktop, Mobile, Navigation, relevante Public-Routen, Admin-Login und eingeloggten Dashboard-Header erfolgreich geprüft.
- **Weitere Pre-Go-live-Gates offen:** finales Impressum, Datenschutz und Betreiberangaben einpflegen; Produktivdomain und `NEXT_PUBLIC_SITE_URL` prüfen; Indexierung erst auf der finalen Domain aktivieren und `robots`/Sitemap erneut prüfen; SMTP, SPF, DKIM, DMARC, From/Reply-To sowie Invite-/Recovery-/Notification-Smoke auf der finalen Domain verifizieren; B15.18J2 Beitragsreminder inklusive Cron-Secret/Vault, Produktivendpunkt, Idempotenz, Monitoring und Rotation separat aktivieren; anschließend vollständigen Desktop-/Tablet-/Mobile-Go-live-Smoke für Public, Dashboard, Rollen, Membership, Mail, Consent, externe Inhalte, Formulare, 404 und Empty States durchführen. Temporäre Tunneladressen sind kein Produktivvertrag.
- **P7.09 Live-Schema-Korrektur:** `public.coach_staff_roles` existiert live nicht; die Relation war lediglich als früheres additives Zielmodell dokumentiert. Der Person-Count verwendet die tatsächliche saisonale Trainer-/Betreuerzuordnung `coach_team_seasons`; bestehende `coaches.role*`-Felder bleiben Fallback. Die Korrektur wurde live erfolgreich bestätigt. Keine DB- oder Produktänderung.
- **Priority-7-Mediaentscheidung:** Der Superadmin-Login und seine relationale Auth-/Profil-/Rollenbindung bleiben zwingend erhalten; sein aktuelles Profilbild ist dagegen ausdrücklich zur späteren Media-/Storage-Löschung freigegeben. Die nullable Media-FK verwendet `ON DELETE SET NULL`; Avatar-Usage und Objekt werden erst im getrennten, manifestgestützten Media-Schritt behandelt. Der direkte Closure-Period-Count ist 0, der fail-closed Null-Guard bleibt bestehen.
- `players.team_id`, `coaches.team_id`, saisonale Relationen und Snapshotfelder.
- `team_template_id`, Legacyfelder und englische Altspalten.
- ausschließlich nach Read-/Write-Audit, Live-Preflight und gesonderter Freigabe.

## Priorität 8 – Technischer Cleanup

- Die veralteten Annahmen in `teamCoachAssignments.core.test.mjs` und den beiden News-UI-Strukturtests sind korrigiert. Der aktuelle dokumentierte B15.23E-Abschlusslauf umfasst 1046/1046 bestandene Tests.
- ESLint-Bestand, Dependency-Audit, Dateigrößen und Architektur neu inventarisieren; keine alten Zahlen ungeprüft übernehmen.

## Priorität 9 – Spätere große Module

- Ticketsystem, Turnierverwaltung, Community/Tauschbörse.
- PWA, native Android-/iOS-App und Social-Media-Automatisierung.

## Priorität 10 – Go-live

- dauerhaftes Hosting, finale Domain, SSL und Mailserver.
- Umgebungsvariablen, Supabase-Produktivkonfiguration und Auth Redirect URLs.
- Einladungsmails, Passwort-Reset, Recovery und Session-Cookies.
- bestehende Testdaten vor Produktivstart kontrolliert bereinigen und anschließend Echtdaten einspielen; Datenschutz, Impressum, Cookie-/Trackingprüfung und Jugendschutzdarstellung abnehmen.
- vollständige Rollenabnahme und Tests auf mobilen Endgeräten.
- Contribution Reminder: `CONTRIBUTION_REMINDER_CRON_SECRET` im Hosting setzen, identisches Secret im Supabase Vault hinterlegen, finalen Produktiv-Endpunkt verwenden und keine temporäre `trycloudflare.com`-Adresse einsetzen.
- finalen Idempotenz-Preflight/Postcheck ausführen, Cron aktivieren, ersten Lauf überwachen, Audit kontrollieren, Rollen-Livetest und Secret-Rotation durchführen und die Funktion danach offiziell freigeben.

## Abgeschlossene größere Entwicklungsblöcke

Siehe [Abgeschlossene Entwicklungsblöcke](completed-development-blocks.md). B15.18 und B15.19 sowie deren dort aufgeführte Security-Nachläufe sind keine offenen Roadmap-Punkte mehr.
